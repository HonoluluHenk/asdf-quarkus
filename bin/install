#!/usr/bin/env bash
set -euo pipefail

script_dir=$(dirname "$0")
# shellcheck source=./functions
source "${script_dir}/functions"

install "$ASDF_INSTALL_TYPE" "$ASDF_INSTALL_VERSION" "$ASDF_INSTALL_PATH" "$ASDF_DOWNLOAD_PATH"
